# Ghostty configuration

theme = TokyoNight

window-height = 50
window-width = 130

font-size = 11

# Alternate 'g' in Jetbrains Mono font
font-feature = cv03
# Alternate '$' in Jetbrains Mono font
font-feature = cv14

# Try to install ghostty on remote, but if that fails, set env to xterm-256color
shell-integration-features = ssh-terminfo,ssh-env

# Load optional config if present
config-file = ?local-config
